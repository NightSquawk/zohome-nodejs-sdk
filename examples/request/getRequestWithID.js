import { ManageEngine } from "../../lib/index";

// Will attempt to establish a connection with the ManageEngine API using the provided credentials
// First will create a new instance of the ManageEngine class
ManageEngine.initialize("{client_id}", "{client_secret}", "{refresh_token}", "{base_url}");

